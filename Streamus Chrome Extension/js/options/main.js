require([
    'jquery',
    'backbone',
    'underscore',
    'jqueryUi'
], function ($, Backbone, _) {
    'use strict';

    //  Load this once everything else is ready.
    require(['options']);
});